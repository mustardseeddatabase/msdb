%li
  = form_for @summary_report, :url => summary_report_path(:format => 'docx'), :method => :get do |f|
    Summary report for:
    = f.date_select :for_date, :discard_day => true, :end_year => Time.now.year
    = f.submit "Generate report"
